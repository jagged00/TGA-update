# Jianghu Dynasty v2.2.7 - Vanilla Map Rescue

- Disabled the unstable total conversion map by moving active map data into `_disabled_total_conversion_map/`.
- Disabled custom bookmarks, custom landed title replacement, province history, and title history.
- Archived unsafe generated vanilla override content and legacy TC/debug/review content.
- Removed dangerous `replace_path` entries from `descriptor.mod` and the launcher `.mod` file.
- Removed the corrupted thumbnail reference instead of pointing the launcher at a broken PNG.
- Switched the stable line to a vanilla-map gameplay overhaul path.

Known issue: the full Song map is postponed to a future v3.0.0 experimental workspace.
